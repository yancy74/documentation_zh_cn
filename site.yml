site:
  title: Circle Linux 文档
  start_page: 8-docs::index
content:
  sources:
   - url: .
     branches: HEAD
ui:
  bundle:
    url:   ./build/ui-bundle.zip
    snapshot: true
  default_layout: default
output:
  clean: true
  dir: ./public
  destinations:
  - provider: archive
runtime:
  pull: true
  cache_dir: ./cache
